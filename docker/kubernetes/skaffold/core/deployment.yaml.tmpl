apiVersion: apps/v1
kind: Deployment
metadata:
  name: core$APPID
  labels:
    app: core$APPID
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core$APPID
  template:
    metadata:
      labels:
        app: core$APPID
    spec:
      containers:
      - name: core
        image: core$IMGID
        ports:
        - containerPort: 8000
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: core$APPID
  labels:
    app: core$APPID
spec:
  type: NodePort
  ports:
  - port: 8000
    name: debug
    protocol: TCP
    targetPort: 8000
  - port: 80
    name: web
    protocol: TCP
    targetPort: 80
  selector:
    app: core$APPID
  sessionAffinity: None  
status:
  loadBalancer: {}    
---
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  labels:
    app.kubernetes.io/component: controller
  name: core$APPID
  annotations:
    ingressclass.kubernetes.io/is-default-class: "false"
spec:
  controller: k8s.io/ingress-nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: name-virtual-host-ingress-core$APPID
spec:
  ingressClassName: core$APPID
  rules:
  - host: $MYSERVER
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: core$APPID-debug
            port:
              number: 8000
          service:
            name: core$APPID
            port:
              number: 80
