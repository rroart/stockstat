apiVersion: apps/v1
kind: Deployment
metadata:
  name: iweba
  labels:
    app: iweba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iweba
  template:
    metadata:
      labels:
        app: iweba
    spec:
      containers:
      - name: iweba
        image: iweba
        #imagePullPolicy: Never
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: iweba
  labels:
    app: iweba
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: iweba
  sessionAffinity: None  
status:
  loadBalancer: {}    
---
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  labels:
    app.kubernetes.io/component: controller
  name: stockstat-iweba
  annotations:
    ingressclass.kubernetes.io/is-default-class: "false"
spec:
  controller: k8s.io/ingress-nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: name-virtual-host-ingress-iweba
spec:
  ingressClassName: stockstat-iweba
  rules:
  - host: $MYWEB
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: iweba
            port:
              number: 80
