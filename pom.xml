<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>roart</groupId>
  <artifactId>stockstat-parent</artifactId>
  <packaging>pom</packaging>
  <version>0.6-SNAPSHOT</version>
  <name>Stock Statistics Parent</name>
  <modules>
    <module>common</module>
    <module>eureka</module>
    <module>iclij</module>
    <module>input</module>
    <module>main</module>
    <module>web</module>
    <module>webr</module>
    <module>distribution</module>
  </modules>
  <build>
    <pluginManagement>
      <plugins>
	<plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${mavencompiler.version}</version>
          <configuration>
            <source>${jdk.version}</source>
            <target>${jdk.version}</target>
            <encoding>UTF-8</encoding>
          </configuration>
	</plugin>
	 <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M5</version>
         </plugin>
	 <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.0.0-M5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

      </plugins>
    </pluginManagement>
  </build>
  <dependencies>
    <!--dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency-->
    <!--dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgresql.version}</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-core</artifactId>
      <version>${hibernate.version}</version>
    </dependency>
    <dependency>
      <groupId>javax.transaction</groupId>
      <artifactId>jta</artifactId>
      <version>1.1</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-ehcache</artifactId>
      <version>${hibernate.version}</version>
    </dependency>
    <dependency>
      <groupId>net.sf.ehcache</groupId>
      <artifactId>ehcache-core</artifactId>
      <version>${ehcache.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>${slf4j.version}</version>
    </dependency>          
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>${log4j.version}</version>
    </dependency-->
  </dependencies>
  <properties>
    <stockstat.version>0.6-SNAPSHOT</stockstat.version>
    <jdk.version>17</jdk.version>
    <jdk.spark.version>17</jdk.spark.version>
    <mavencompiler.version>3.8.1</mavencompiler.version>
    <maveninstall.version>2.5.2</maveninstall.version>
    <mavenshade.version>3.2.1</mavenshade.version>
    <jetty.version>9.4.12.v20180830</jetty.version>
    <springboot.version>2.5.4</springboot.version>
    <springcloud.version>3.0.3</springcloud.version>
    <springcloudstarter.version>2020.0.3</springcloudstarter.version>
    <springcore.version>5.3.9</springcore.version>
    <eurekaclient.version>1.9.8</eurekaclient.version>
    <jackson.version>2.12.5</jackson.version>
    <hibernate.version>5.5.7.Final</hibernate.version>
    <hibernate.dialect>org.hibernate.dialect.H2Dialect</hibernate.dialect>
    <hibernate.hbm2ddl.auto>create</hibernate.hbm2ddl.auto>
    <ehcache.version>2.6.11</ehcache.version>
    <slf4j.version>1.7.32</slf4j.version>
    <postgresql.version>42.2.11</postgresql.version>
    <junit.version>5.7.2</junit.version>
    <log4j.version>1.2.17</log4j.version>
    <commons.version>2.1.1</commons.version>
    <commons.math3.version>3.6.1</commons.math3.version>
    <commons.lang3.version>3.8.1</commons.lang3.version>
    <commons.codec.version>1.15</commons.codec.version>
    <commons.beanutils.version>1.9.4</commons.beanutils.version>
    <httpclient.version>4.5.6</httpclient.version>
    <jaxb.version>2.3.0</jaxb.version>
    <stanfordcorenlp.version>3.9.1</stanfordcorenlp.version>
    <opennlp.version>1.8.4</opennlp.version>
    <batik.version>1.8</batik.version>
    <jfreesvg.version>3.0</jfreesvg.version>
    <jfreechart.version>1.0.19</jfreechart.version>
    <vaadin.version>7.6.0</vaadin.version>
    <vaadin.plugin.version>${vaadin.version}</vaadin.plugin.version>
    <servlet.version>3.1.0</servlet.version>
    <xmlapis.version>1.4.01</xmlapis.version>
    <spark.version>3.1.2</spark.version>
    <talib.version>0.4.0</talib.version>
    <jenetics.version>5.2.0</jenetics.version>
    <guava.version>16.0.1</guava.version>
    <camel.version>3.6.0</camel.version>
    <camelactivemq.version>5.16.0</camelactivemq.version>
    <springintegration.version>5.4.0</springintegration.version>
    <springrabbit.version>2.3.1</springrabbit.version>
    <kafka.version>2.6.0</kafka.version> 
    <pulsar.version>2.6.1</pulsar.version>
    <hazelcast.version>3.12.12</hazelcast.version>
    <hazelcastclient.version>3.2.12</hazelcastclient.version>
    <redisclient.version>3.5.1</redisclient.version>
    <!-- need old version -->
    <janino.version>3.0.16</janino.version>
    <tweety.version>1.20</tweety.version>
 </properties>
</project>
