(function (window) {
      window.env = window.env || {};
      // Variables
      window["env"]["MYSERVER"] = "$MYSERVER";
      window["env"]["MYPORT"] = "$MYPORT";
      window["env"]["MYISERVER"] = "$MYISERVER";
      window["env"]["MYIPORT"] = "$MYIPORT";
      window["env"]["MYASERVER"] = "$MYASERVER";
      window["env"]["MYAPORT"] = "$MYAPORT";
})(this);
